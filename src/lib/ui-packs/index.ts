import type { UIStringsOverride } from "../i18n-ui";
import { ar } from "./ar";

/** Built-in sparse language packs, keyed by BCP 47 locale code. */
export const UI_PACKS: Record<string, UIStringsOverride> = { ar };
