---
// Keep the old root URL useful while making every rendered page locale-prefixed.
// Render only this route on demand so production returns a real HTTP redirect.
export const prerender = false;

return Astro.redirect("/en/", 302);
---
